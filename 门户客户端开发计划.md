# PPT 数字人课程门户客户端开发计划

## 一、目标

使用 React 技术栈开发一个面向教师和内容制作人员的门户客户端：用户上传 PPT/PDF，按页面编辑讲稿、数字人、音色和画面布局，提交合成任务，最后预览和下载数字人课程视频。

客户端采用产品门户形态，不沿用 `easegen-front` 的后台管理布局、侧边菜单、权限菜单和 Element Plus 管理表格。

## 二、调研结论

### 参考项目提供的主流程

`easegen-front` 的核心制作流程集中在 `src/views/chooseTemplate/index.vue`：

1. 上传 PPT/PDF。
2. 调用文件上传接口，填写课件说明。
3. 后端解析课件并轮询解析进度。
4. 生成逐页场景缩略图和中央画布。
5. 每页编辑背景、画中画、数字人位置和尺寸。
6. 每页选择文字讲稿或用户音频。
7. 选择数字人和声音模型。
8. 保存课程草稿。
9. 校验场景后提交视频合成。
10. 在“我的课程”中查看状态、预览和下载。

参考项目还提供了 Docmee AI PPT 嵌入页面、数字人资源管理、音色管理和 3D 课程页面。这些页面属于后台或资源运营功能，门户客户端第一期不直接照搬。

### 可复用的业务模型

`easegen-admin` 已有课程领域模型：

- 课程：`courses`
- 课程 PPT：`courseppts`
- 课程场景：`coursescenes`
- 场景文本：`coursescenetexts`
- 场景音频：`coursesceneaudios`
- 场景背景：`coursescenebackgrounds`
- 场景组件：`coursescenecomponents`
- 场景声音：`coursescenevoices`
- 数字人：`digitalhumans`
- 音色：`voices`
- 模板：`template`
- 合成任务：`coursemedia`

Java 后端已提供课程、PPT 解析、数字人、音色、模板和合成任务接口。当前 Python 服务负责具体的语音、SadTalker、Wav2Lip 和视频合成，应通过后端服务统一调用。

## 三、推荐技术方案

### 当前一期真实处理管线

一期以原始 PPT/PPTX 为唯一用户输入，固定人物图片和声音模型，不要求用户上传音频或导出视频：

```text
PPT/PPTX
  → LibreOffice/PDFBox 解析
  → 页面图片 + 页面文本/备注
  → 课程场景
  → LLM 生成讲稿
  → 用户编辑讲稿
  → CosyVoice 或 GPT-SoVITS 生成语音
  → 音频与字幕时间轴
  → MuseTalk 或 SadTalker + 固定人物图
  → 透明/绿幕人物视频
  → FFmpeg 合成课件与数字人
  → MP4 + SRT/VTT
  → 浏览器下载服务器本地文件
```

一期不接入 MinIO 或 OSS。生成文件保存在云服务器课程目录，通过下载接口或受控文件流返回给浏览器。前端页面只保留“上传课件、编辑讲稿、生成课程、下载结果”四个用户动作。

### 前端

- React 18+
- Vite
- TypeScript
- React Router
- TanStack Query：请求缓存、轮询和任务状态管理
- Zustand：当前课程编辑状态、用户状态和临时上传状态
- Axios：HTTP 客户端
- Ant Design 或 shadcn/ui：门户组件基础
- React Hook Form + Zod：表单和配置校验
- `react-pdf` 或后端生成的页面缩略图：课件预览
- 原生 `<video>`：视频预览

建议使用独立项目目录，例如：

```text
/Volumes/Marisa_Data_A/3d_human/digital-human-portal
```

### 前后端边界

门户客户端只依赖一个统一业务 API 地址。建议由 Java 后端提供 BFF/适配层，屏蔽 Python API 的账号密码、文件格式差异和 Base64 返回值。

```text
React 门户
    ↓ 登录、课程、资源、任务接口
Java 业务后端
    ↓ 内部调用
Python 数字人推理服务
```

第一期如果暂时无法增加 BFF，则在前端建立 `courseApi` 和 `digitalHumanApi` 两层适配器，禁止在组件中直接拼接 Python 接口。

## 四、门户信息架构

### 公开门户

- `/`：产品首页，展示“上传 PPT 生成数字人课程”的核心入口。
- `/templates`：课程模板和示例课程。
- `/login`：登录。

### 登录后工作区

- `/workspace`：最近课程、继续编辑、合成任务状态。
- `/courses`：我的课程，支持搜索、状态筛选、预览和下载。
- `/courses/new`：新建课程向导。
- `/courses/:id/edit`：课程编辑器。
- `/courses/:id/preview`：课程预览和导出。
- `/profile`：个人资料和基础设置。

第一期不提供后台管理菜单、字典管理、用户管理、数字人审核、音色审核、订单和 CRM 等页面。

## 五、课程制作页面设计

### 1. 新建课程向导

页面采用三步或四步卡片式流程：

1. 上传课件：支持 `.pptx`、`.pdf`，显示大小和格式校验。
2. 选择风格：选择画面比例、模板、背景和默认数字人。
3. 配置讲解：确认每页讲稿、音色和数字人。
4. 开始生成：显示校验结果、预计时长和合成提示。

上传后显示解析进度，解析失败允许重试；解析中的课程可以离开页面，回来后继续轮询。

### 2. 课程编辑器

采用门户工作台布局：

- 顶部：返回、课程名称、保存草稿、生成视频。
- 左侧：页面缩略图列表、排序、复制、删除。
- 中间：16:9 或 4:3 画布，编辑 PPT、画中画和数字人位置。
- 右侧：数字人、背景、模板和布局属性面板。
- 底部或中下部：讲稿编辑、音色选择、语速、音量和试听。

编辑状态以 `courseDraft` 为唯一来源，避免缩略图、画布和右侧表单各自维护一份数据。

### 3. 我的课程

课程卡片显示：封面、名称、页数、预计时长、创建时间、合成状态和进度。

支持：

- 继续编辑草稿；
- 查看合成进度；
- 预览成片；
- 下载视频；
- 失败后重新合成；
- 删除课程。

## 六、前端业务状态设计

```ts
type CourseStatus =
  | 'draft'
  | 'parsing'
  | 'ready'
  | 'synthesizing'
  | 'completed'
  | 'failed'

type Scene = {
  id: string
  orderNo: number
  background: BackgroundConfig
  components: SceneComponent[]
  script: string
  driverType: 'text' | 'audio'
  audioUrl?: string
  voiceId?: number
}
```

TanStack Query 管理服务端数据；Zustand 只管理当前编辑过程中的草稿、选中页、上传进度和 UI 状态。保存时将草稿转换为后端 `courses + scenes` 结构。

## 七、接口适配计划

### 现有 Java 后端优先使用

- 课程创建、详情、保存、分页、删除；
- PPT 上传、解析、解析进度和取消解析；
- 数字人分页和详情；
- 音色分页和试听；
- 模板分页和详情；
- 合成任务创建、任务状态、重新合成；
- 视频预览地址和下载地址。

### Python 数字人接口由适配层封装

如果 Java BFF 暂未完成，前端适配器至少需要封装：

- `Login`；
- `Send_Image`、`Send_Video`、`Send_PPT_Audio`；
- `Get_Inference_VITS_Sadtalker`；
- `Get_Inference_User_Audio_Sadtalker`；
- `PPT_Video_Merge`；
- `Get_State`；
- `Pull_Video_Merge`。

前端不直接处理 `User/Password`、服务器文件路径和 Base64 视频解码细节。视频下载应由适配层转换为 Blob URL 或后端文件 URL。

## 八、分阶段开发计划

### 阶段 0：接口和数据确认（1 天）

- 确认 Java 后端 API 前缀、登录方式和文件上传方式。
- 确认 Python 服务的内网地址、健康检查和任务状态协议。
- 确认视频、缩略图和音频的最终访问 URL。
- 确认课程状态枚举和错误信息格式。

交付物：接口映射表、环境变量说明、端到端请求样例。

### 阶段 1：React 门户骨架（1—2 天）

- 初始化 Vite + React + TypeScript。
- 配置路由、请求层、环境变量、登录态和错误提示。
- 完成首页、登录页、工作区和空状态页面。
- 建立门户视觉规范：颜色、卡片、间距、按钮和响应式断点。

交付物：可运行门户壳、登录后路由保护、基础布局。

### 阶段 2：课程列表和新建课程（2—3 天）

- 接入课程分页、详情和删除。
- 实现上传 PPT/PDF、格式校验和上传进度。
- 实现解析进度轮询、失败重试和取消解析。
- 实现新建课程基本信息和模板选择。

交付物：用户能够上传课件并进入编辑器。

### 阶段 3：课程编辑器 MVP（4—6 天）

- 实现页面缩略图、排序、复制和删除。
- 实现 PPT 画布预览。
- 实现数字人选择、位置和尺寸编辑。
- 实现背景、画中画和模板配置。
- 实现逐页讲稿编辑、音色选择、语速和音量。
- 实现草稿保存和恢复。

交付物：能够完整编辑一个课程草稿。

### 阶段 4：试听和数字人视频合成（3—4 天）

- 接入文本转语音试听。
- 接入 SadTalker 推理任务。
- 接入合成任务创建和轮询。
- 处理任务失败、重试、超时和离开页面后恢复。
- 实现结果视频预览和下载。

交付物：PPT → 数字人课程视频的最小闭环。

### 阶段 5：门户体验完善（2—3 天）

- 首页示例课程和模板展示。
- 最近课程、继续编辑和任务状态卡片。
- 空状态、错误状态和上传失败引导。
- 移动端和窄屏适配，至少保证编辑器在桌面端稳定。
- 统一埋点和前端错误日志。

交付物：可供内部试用的门户客户端。

### 阶段 6：验收和部署（2 天）

- 完成 TypeScript、ESLint、构建检查。
- 使用真实 PPT、长讲稿、多页课件进行验收。
- 验证大文件上传、刷新恢复、重复提交和并发任务。
- 配置生产 API 地址、CORS、文件域名和下载策略。
- 输出部署说明和用户操作说明。

## 九、验收标准

- 用户无需进入后台管理页面即可完成全流程。
- PPT/PDF 上传、解析和进度展示可用。
- 至少 10 页课件可以逐页编辑并保存。
- 每页可以选择讲稿或音频，并选择数字人和音色。
- 合成任务可以显示进行中、成功和失败状态。
- 成功后可以在线播放和下载视频。
- 刷新浏览器或重新进入课程后，草稿和任务状态可以恢复。
- 所有核心请求有明确错误提示，不出现静默失败。

## 十、主要风险和处理建议

1. **Python 服务接口与 Java 课程接口模型不同**：优先建设 Java BFF，统一任务 ID、状态和文件 URL。
2. **PPT 解析耗时较长**：使用任务状态轮询，不阻塞页面；轮询间隔建议 2 秒并支持退避。
3. **视频 Base64 体积大**：生产环境改为文件 URL 或对象存储 URL，避免 JSON 返回整段视频。
4. **当前 Python 服务是 Flask 开发服务器**：正式环境使用 Gunicorn/gevent，并增加健康检查和任务日志。
5. **模型推理占用 GPU**：前端必须防止重复提交，后端需要任务幂等和并发限制。
6. **参考项目是 Vue 后台模板**：只复用领域模型、接口语义和编辑流程，重新设计 React 门户的信息架构和视觉层。

## 十一、第一期建议范围

第一期只做 2D 数字人课程闭环：

```text
登录 → 上传 PPT/PDF → 解析 → 逐页编辑讲稿
→ 选择数字人和音色 → 保存 → 合成 → 预览/下载
```

3D 演播室、AI 自动生成 PPT、题目生成、数字人审核、音色管理、模板运营和支付体系放到后续版本，避免首期范围失控。
