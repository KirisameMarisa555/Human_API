# 施工进度

## 今日总结（30字）

已登录测试服务器确认权重有效，SadTalker视频已生成并下载，本地抽帧验证画面正常

## 已完成

- AutoDL RTX 3080 Ti 云端实例已创建并可用。
- PyTorch 2.1.2 + CUDA 11.8 已验证，GPU 可用。
- SadTalker、Easy-Wav2Lip 模型及依赖已部署。
- GPT-SoVITS 模块、男声模型和参考音频已加载成功。
- 已生成测试语音并开始 SadTalker 视频推理。
- 已发现 GFPGAN 自动下载文件不完整，定位为权重损坏。
- 已确认 GFPGAN 权重已覆盖且可由 `torch.load` 正常读取（权重文件约 333M）。
- 已确认 SadTalker 已生成 `/root/autodl-tmp/outputs/test_video/Image##test_full.mp4`，时长 3.84 秒、含音频。
- 已将成片下载到 `Result/SadTalker/test_video.mp4`，抽帧检查人物画面和嘴部运动正常。

## 下次继续

1. 准备最小端到端测试数据，验证 `Main.py` 的用户音频 + SadTalker 流程。
2. 检查并启动 `server1.4.0.py`，确认 Flask API 可用。
3. 再接入 PPT 批注、语音和视频合成流程。

## 交接记录（2026-09-21）

- 云端代码：`/root/Digital_Human_API`；本文件保存在本地工作区。
- 环境激活：`source /root/autodl-tmp/sadtalker-env/bin/activate`。
- 输入照片：`/root/autodl-tmp/inputs/Image.png`，1728×2304 RGB。
- 测试音频：`/root/autodl-tmp/outputs/test.wav`；从后续视频推理能读取音频判断已生成，尚未记录试听结果。
- 目标视频：`/root/autodl-tmp/outputs/test_video.mp4`；尚未验证最终成片成功。
- 完整 GFPGAN 权重候选：`/root/Digital_Human_API/Easy_Wav2Lip/checkpoints/GFPGANv1.4.pth`，约 333M；来自通过外层 ZIP 校验的模型包，仍应实际加载验证。
- 损坏目标：`/root/autodl-tmp/sadtalker-env/lib/python3.10/site-packages/gfpgan/weights/GFPGANv1.4.pth`。最后报错为 `unexpected EOF`。已经提供覆盖命令，但用户尚未反馈执行结果，不能视为已修复。
- LangSegment 0.2.0 的 `__init__.py` 已移除不存在的 `setLangfilters`、`getLangfilters` 导入，属于临时兼容修补。
- HuBERT 加载曾提示 `weight_g/weight_v` 未使用、`parametrizations.weight.original0/original1` 新初始化。此问题不能仅因模块导入成功而忽略；后续需核对权重映射和语音质量。
- 之前安装曾将 NumPy 升到 2.x，随后给出恢复 1.23.5 的命令；下次保存实际环境版本并检查依赖冲突。
- 用户希望命令短且逐行执行，避免长 `python -c` 因粘贴换行而出错；建议后续使用独立测试脚本。
- 尚未完成 Flask API、PPT 全流程或端到端验收；不要把模型初始化成功等同于完整服务部署成功。
- 停止工作后需在 AutoDL 控制台确认是否关机，避免持续 GPU 计费；本次未代操作关机。

## 继续记录（2026-09-22）

- 已通过 SSH 登录测试服务器 `connect.nmb2.seetacloud.com:29143`。
- 当前环境：Python 3.10.8、PyTorch 2.1.2+cu118、CUDA 可用、NumPy 1.23.5。
- 发现命令行环境存在 `OMP_NUM_THREADS` 无效值提示，不影响当前推理；后续启动服务前应清理该环境变量。
- 本地验证文件：`Result/SadTalker/test_video.mp4`；抽帧位于 `Result/SadTalker/frames/`。
- 已确认 `server1.4.0.py` 与 `Main.py` 语法检查通过；虚拟环境已补装 Flask 3.1.0、Flask-Cors 4.0.0。
- 已完成 `rembg` 安装并验证导入成功；`server1.4.0.py` 在清理 `OMP_NUM_THREADS` 后已完成实际导入，输出 `server import ok`。导入时仍有 HuBERT 权重映射提示，需后续做语音质量验收。
- 修复 `Get_Test_Inference`、`Get_Inference_VITS` 对 `Get_Parmes()` 返回值数量解包错误，并已同步到云端。
- 已在云端启动 `server1.4.0.py`（PID 3738），监听 `0.0.0.0:5000`；`/Login` 使用测试账号请求返回 `{"result":"Success"}`。
- `/Get_State` 在任务尚未写入状态时返回 `Failed`，属于当前接口实现行为；需要先提交对应任务后再查询。
- 已创建 React 门户项目 `/Volumes/Marisa_Data_A/3d_human/digital-human-portal`，完成 Vite、TypeScript、TanStack Query、Axios、Zustand 基础接入。
- 已配置浏览器 `/api` → Vite 代理 → 本地 5000 → SSH 隧道 → 云端 5000；本地隧道验证可收到云端 API 响应。
- React 门户首期基础页已完成登录、素材上传、推理任务、视频合成、状态轮询和视频下载入口，生产构建已通过。

## 继续记录（2026-09-22 下午）

- 按门户产品流程重构客户端：用户只上传原始 PPT/PPTX/PDF，数字人图片和声音改为固定系统预设。
- 新增课件制作向导页面：上传课件、编辑逐页讲稿、提交生成、查看结果。
- 新增课程接口契约文档 `课程接口契约.md`，明确上传、解析、场景保存、渲染、状态查询和本地文件下载接口。
- 在 `server1.4.0.py` 新增 `/Course_Upload`、`/Course_Parse`、`/Course_Scenes`、`/Course_Render`、`/Course_State`、`/Course_Download`。
- `/Course_Parse` 已支持调用 LibreOffice 转 PDF、`pdftoppm` 生成页面图片、`pdftotext` 提取文本，并为 PPTX 增加 XML 文本兜底提取。
- 新接口代码已通过 `python3 -m py_compile`，并同步到云端 `/root/Digital_Human_API/server1.4.0.py`。
- 云端重启时发现服务器没有 `python` 命令，服务尚未重新启动；下一次应使用实际虚拟环境中的 `python3` 或环境绝对路径启动后再做接口验收。
- 当前课程解析和数字人完整合成仍需接入 LLM、CosyVoice/GPT-SoVITS、SadTalker/MuseTalk 与 FFmpeg 编排任务；OSS/MinIO 暂不接入，结果先从服务器本地下载。

## 后续施工计划（2026-09-23）

### 阶段 A：恢复云端服务并完成接口基线

1. 使用虚拟环境中的 `python3` 重启 `server1.4.0.py`，清理无效的 `OMP_NUM_THREADS`，记录进程、端口和日志位置。
2. 验收 `/Login`、`/Course_Upload`、`/Course_Parse`、`/Course_Scenes`、`/Course_State`、`/Course_Download` 的真实请求和错误响应。
3. 固化课程接口字段：统一使用 `Course_Id`、`Task_Id`、`User`，补充任务状态枚举和失败信息；前端文档中的旧接口名同步修正。
4. 为课程目录增加按 `course_id` 隔离的存储，避免同一用户重复上传覆盖旧课程。

交付物：可重复执行的接口验收脚本、接口映射表、云端启动说明。

### 阶段 B：打通课件上传与页面编辑

1. 将 React 门户从本地演示数据切换到 `Course_Upload` 和 `Course_Parse`。
2. 解析任务改为轮询 `Course_State`，显示解析中、成功、失败和重试。
3. 使用服务端返回的页面图片、文本和讲稿草稿构建真实缩略图与编辑器。
4. 接入 `GET/PUT /Course_Scenes`，实现逐页讲稿保存、刷新恢复和离开页面后继续编辑。

交付物：上传真实 PPT/PDF 后能够进入真实页面编辑器的门户版本。

### 阶段 C：实现课程级生成编排

1. 明确每页渲染输入和输出目录，建立 `parse → script → tts → avatar → merge` 五阶段状态机。
2. 先接入现有 GPT-SoVITS 生成单页音频，再生成字幕时间轴（VTT/SRT）。
3. 调用已验证的 SadTalker，使用固定人物图片生成每页数字人视频。
4. 使用 FFmpeg 将 PPT 页面、数字人视频和音频按页合成，最后合并为 `last_video.mp4`。
5. 将 `Course_Render` 改为后台任务，返回任务 ID；支持重复提交保护、失败状态和重试。

交付物：一页课件的真实生成闭环，再扩展到多页课程。

### 阶段 D：门户结果页和异常恢复

1. React 接入 `Course_Render`、`Course_State` 和 `Course_Download`，移除本地 `setTimeout` 假任务。
2. 增加生成进度、阶段提示、失败原因、重新生成、视频预览和 VTT/MP4 下载。
3. 保存当前课程 ID 和编辑草稿，刷新浏览器后恢复课程和任务状态。
4. 防止重复点击生成；处理超时、断网、文件格式错误和服务器 5xx。

交付物：PPT/PDF → 编辑讲稿 → 数字人 MP4/VTT 的最小可用闭环。

### 阶段 E：真实数据验收与部署

1. 使用 1 页、10 页、长讲稿和包含备注的 PPT/PDF 做回归测试。
2. 检查中文语音质量、字幕时间轴、嘴型同步、画面比例和音画时长。
3. 记录 GPU 显存、单页耗时、整课耗时和并发限制，补充任务日志。
4. 将 Flask 开发服务器切换为 Gunicorn/gevent，增加健康检查和受控下载。
5. 更新 README、环境变量、启动命令和模型权重下载说明，并将最终变更推送到 API 与门户仓库。

## 当前执行顺序

优先完成阶段 A，然后做阶段 B 的真实上传和解析；阶段 C 先用单页验证渲染编排，成功后再接入多页和门户结果页。模型权重继续保留在云端，不提交到 Git 仓库。

## 阶段 A 执行记录（2026-09-23）

- 已将 `Main.py` 中 SadTalker、GPT-SoVITS、Wav2Lip 和合成模块改为按请求懒加载。
- 修复前服务启动时因顶层加载 GPT-SoVITS 大模型被系统以退出码 137 杀掉；修复后服务已稳定启动。
- 云端启动命令：`unset OMP_NUM_THREADS && /root/autodl-tmp/sadtalker-env/bin/python3 -u server1.4.0.py`。
- 当前服务 PID 为 `1689`，监听 `0.0.0.0:5000`，日志位于 `/root/Digital_Human_API/logs/server.log`。
- `/Login` 返回成功；课程上传、解析、场景读取、场景保存、状态查询和下载错误响应均已完成真实请求验收。
- 云端尚未安装 LibreOffice、`pdftoppm`、`pdftotext`；当前 `/Course_Parse` 只能写入空页面占位场景，真实 PPT/PDF 页面图片和文本解析仍被阻塞。
- `/Course_Render` 当前只创建占位状态，随后 `/Course_State` 返回 `false`；真实语音、数字人和 FFmpeg 编排列入阶段 C。
- 下一步先补齐课件解析依赖并改造按 `course_id` 隔离的课程存储，再进入门户真实上传和编辑接入。

## 解析依赖与课程隔离（2026-09-23）

- 云端已安装 `libreoffice-impress` 和 `poppler-utils`，用于 PPT/PPTX 转 PDF、页面渲染和文本提取；额外占用约 274 MB 磁盘空间。
- 课程接口已改为每次上传生成独立的 `Course_Id`，目录结构为 `Data/<User>/Courses/<Course_Id>/`。
- `Course_Parse`、`Course_Scenes`、`Course_Render`、`Course_State`、`Course_Download` 均要求携带 `Course_Id`，不会再覆盖同一用户的其他课程。
- 已同步云端并重启服务，当前服务 PID 为 `3737`。
- 因当前服务器处于无 GPU 模式，本轮只完成依赖、目录隔离和服务加载，不进行课件解析或模型推理测试；待切换 GPU 后再做真实文件验收。

## CPU 课件解析与隔离验收（2026-09-23）

- 使用真实 PDF 完成 CPU-only 解析，已生成 `page-1.png` 和页面文本 `Stage A parsing test`。
- 修复 `pdftotext` 末尾分页符被误算为空场景的问题；一页 PDF 现在返回一个场景。
- 同一用户创建课程 `69637099a4ce`、`3840a27cdefb`，两者均可独立上传、解析和读取场景。
- 修改第一个课程的讲稿后，第一课程返回新讲稿，第二课程仍保持空讲稿，确认 `Course_Id` 隔离生效。
- 解析与隔离验收未加载 GPU 模型；云端服务当前 PID 为 `4010`。

## 真实任务状态链（2026-09-23）

- `/Course_Render` 已改为后台任务，返回 `task_id` 和状态对象，不再返回无语义的布尔值。
- `/Course_State` 现在返回 `status`（`idle`、`queued`、`running`、`success`、`failed`）、`stage`、`progress`、`task_id` 和 `error`。
- 已增加重复提交保护：同一课程处于 `queued/running` 时返回现有任务。
- 已验证任务可以从 `queued` 进入 `failed`，失败原因会明确返回；当前失败原因是渲染管线尚未接入 GPT-SoVITS、SadTalker 和 FFmpeg。
- React 门户已按状态对象轮询并显示阶段、进度和失败原因。
- 云端服务已重启，当前 PID 为 `1386`；GPU 已恢复，下一步接入真实渲染 worker。

## 单页 GPU 渲染 worker（2026-09-23）

- 已接入固定参考音频、GPT-SoVITS、SadTalker 和 FFmpeg 单页渲染流程。
- 任务阶段现在会经过 `validate → tts → gpu_inference → complete`，异常写入 `failed` 和 `error`。
- 已使用课程 `69637099a4ce` 完成真实 GPU 渲染；任务 ID 为 `13470e5b8b4f4832`，最终状态为 `success`。
- 已生成：`Course_Render/page-1.wav`、`Course_Render/page-1-avatar.mp4`、`last_video.mp4` 和 `course.vtt`。
- 最终 MP4 约 1.15 秒、26 KB，`/Course_Download?type=mp4` 返回 200。
- RTX 3080 Ti 推理期间显存约使用 4.45 GB；SadTalker GFPGAN 增强阶段耗时约 53 秒。
- 当前 worker 只渲染第一页，下一步扩展为逐页语音、数字人视频和最终课程合并。

## 多页 GPU 渲染（2026-09-23）

- worker 已改为一次加载 GPT-SoVITS 和 SadTalker，按场景逐页生成 WAV、数字人 MP4 和页面合成 MP4。
- 使用 FFmpeg concat 合并全部页面，按每页实际视频时长生成 VTT 字幕时间轴。
- 已完成两页课程验证：任务 `7a1f4b84303b448e` 最终状态为 `success`。
- 产物包含 `page-1.wav`、`page-2.wav`、`page-1-avatar.mp4`、`page-2-avatar.mp4`、`last_video.mp4` 和双 cue `course.vtt`。
- 最终视频时长 2.24 秒，说明两页已顺序合并；GPU 显存约使用 4.41 GB。
- SadTalker 的 GFPGAN 增强仍是主要耗时环节，当前两页任务约需两分钟。

## 门户刷新恢复与页面缩略图（2026-09-23）

- 新增 `GET /Course_Info`，返回课程名称、场景和最近任务状态。
- 新增 `GET /Course_Page`，按 `User + Course_Id + index` 返回受控页面 PNG，不暴露服务器路径。
- React 门户启动时会根据本地保存的 `Course_Id` 恢复课程名称、场景、讲稿和生成结果状态。
- 编辑器左侧缩略图和中央画布现在加载真实页面图片；单页图片加载失败不会阻断讲稿恢复。
- 已核对课程 `69637099a4ce`：`/Course_Info` 返回两页和 `success` 状态，`/Course_Page?index=1` 返回 HTTP 200。
