# 施工进度

## 今日总结（30字）

已登录测试服务器确认权重有效，SadTalker视频已生成并下载，本地抽帧验证画面正常

## 已完成

- AutoDL RTX 3080 Ti 云端实例已创建并可用。
- PyTorch 2.1.2 + CUDA 11.8 已验证，GPU 可用。
- SadTalker、Easy-Wav2Lip 模型及依赖已部署。
- GPT-SoVITS 模块、男声模型和参考音频已加载成功。
- 已生成测试语音并开始 SadTalker 视频推理。
- 已发现 GFPGAN 自动下载文件不完整，定位为权重损坏。
- 已确认 GFPGAN 权重已覆盖且可由 `torch.load` 正常读取（权重文件约 333M）。
- 已确认 SadTalker 已生成 `/root/autodl-tmp/outputs/test_video/Image##test_full.mp4`，时长 3.84 秒、含音频。
- 已将成片下载到 `Result/SadTalker/test_video.mp4`，抽帧检查人物画面和嘴部运动正常。

## 下次继续

1. 准备最小端到端测试数据，验证 `Main.py` 的用户音频 + SadTalker 流程。
2. 检查并启动 `server1.4.0.py`，确认 Flask API 可用。
3. 再接入 PPT 批注、语音和视频合成流程。

## 交接记录（2026-09-21）

- 云端代码：`/root/Digital_Human_API`；本文件保存在本地工作区。
- 环境激活：`source /root/autodl-tmp/sadtalker-env/bin/activate`。
- 输入照片：`/root/autodl-tmp/inputs/Image.png`，1728×2304 RGB。
- 测试音频：`/root/autodl-tmp/outputs/test.wav`；从后续视频推理能读取音频判断已生成，尚未记录试听结果。
- 目标视频：`/root/autodl-tmp/outputs/test_video.mp4`；尚未验证最终成片成功。
- 完整 GFPGAN 权重候选：`/root/Digital_Human_API/Easy_Wav2Lip/checkpoints/GFPGANv1.4.pth`，约 333M；来自通过外层 ZIP 校验的模型包，仍应实际加载验证。
- 损坏目标：`/root/autodl-tmp/sadtalker-env/lib/python3.10/site-packages/gfpgan/weights/GFPGANv1.4.pth`。最后报错为 `unexpected EOF`。已经提供覆盖命令，但用户尚未反馈执行结果，不能视为已修复。
- LangSegment 0.2.0 的 `__init__.py` 已移除不存在的 `setLangfilters`、`getLangfilters` 导入，属于临时兼容修补。
- HuBERT 加载曾提示 `weight_g/weight_v` 未使用、`parametrizations.weight.original0/original1` 新初始化。此问题不能仅因模块导入成功而忽略；后续需核对权重映射和语音质量。
- 之前安装曾将 NumPy 升到 2.x，随后给出恢复 1.23.5 的命令；下次保存实际环境版本并检查依赖冲突。
- 用户希望命令短且逐行执行，避免长 `python -c` 因粘贴换行而出错；建议后续使用独立测试脚本。
- 尚未完成 Flask API、PPT 全流程或端到端验收；不要把模型初始化成功等同于完整服务部署成功。
- 停止工作后需在 AutoDL 控制台确认是否关机，避免持续 GPU 计费；本次未代操作关机。

## 继续记录（2026-09-22）

- 已通过 SSH 登录测试服务器 `connect.nmb2.seetacloud.com:29143`。
- 当前环境：Python 3.10.8、PyTorch 2.1.2+cu118、CUDA 可用、NumPy 1.23.5。
- 发现命令行环境存在 `OMP_NUM_THREADS` 无效值提示，不影响当前推理；后续启动服务前应清理该环境变量。
- 本地验证文件：`Result/SadTalker/test_video.mp4`；抽帧位于 `Result/SadTalker/frames/`。
- 已确认 `server1.4.0.py` 与 `Main.py` 语法检查通过；虚拟环境已补装 Flask 3.1.0、Flask-Cors 4.0.0。
- 已完成 `rembg` 安装并验证导入成功；`server1.4.0.py` 在清理 `OMP_NUM_THREADS` 后已完成实际导入，输出 `server import ok`。导入时仍有 HuBERT 权重映射提示，需后续做语音质量验收。
- 修复 `Get_Test_Inference`、`Get_Inference_VITS` 对 `Get_Parmes()` 返回值数量解包错误，并已同步到云端。
- 已在云端启动 `server1.4.0.py`（PID 3738），监听 `0.0.0.0:5000`；`/Login` 使用测试账号请求返回 `{"result":"Success"}`。
- `/Get_State` 在任务尚未写入状态时返回 `Failed`，属于当前接口实现行为；需要先提交对应任务后再查询。
- 已创建 React 门户项目 `/Volumes/Marisa_Data_A/3d_human/digital-human-portal`，完成 Vite、TypeScript、TanStack Query、Axios、Zustand 基础接入。
- 已配置浏览器 `/api` → Vite 代理 → 本地 5000 → SSH 隧道 → 云端 5000；本地隧道验证可收到云端 API 响应。
- React 门户首期基础页已完成登录、素材上传、推理任务、视频合成、状态轮询和视频下载入口，生产构建已通过。

## 继续记录（2026-09-22 下午）

- 按门户产品流程重构客户端：用户只上传原始 PPT/PPTX/PDF，数字人图片和声音改为固定系统预设。
- 新增课件制作向导页面：上传课件、编辑逐页讲稿、提交生成、查看结果。
- 新增课程接口契约文档 `课程接口契约.md`，明确上传、解析、场景保存、渲染、状态查询和本地文件下载接口。
- 在 `server1.4.0.py` 新增 `/Course_Upload`、`/Course_Parse`、`/Course_Scenes`、`/Course_Render`、`/Course_State`、`/Course_Download`。
- `/Course_Parse` 已支持调用 LibreOffice 转 PDF、`pdftoppm` 生成页面图片、`pdftotext` 提取文本，并为 PPTX 增加 XML 文本兜底提取。
- 新接口代码已通过 `python3 -m py_compile`，并同步到云端 `/root/Digital_Human_API/server1.4.0.py`。
- 云端重启时发现服务器没有 `python` 命令，服务尚未重新启动；下一次应使用实际虚拟环境中的 `python3` 或环境绝对路径启动后再做接口验收。
- 当前课程解析和数字人完整合成仍需接入 LLM、CosyVoice/GPT-SoVITS、SadTalker/MuseTalk 与 FFmpeg 编排任务；OSS/MinIO 暂不接入，结果先从服务器本地下载。
