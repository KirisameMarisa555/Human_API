# 数字人课程一期接口契约

用户只上传原始 PPT/PPTX/PDF。人物图片和声音模型由服务器预设，生成文件暂存服务器本地。

## 上传与解析

`POST /Course_Upload`

```text
multipart/form-data
File: course.pptx
User: Test
Course_Name: 产品培训
```

响应：`{"result":"Success","course_id":"course_001"}`。

`POST /Course_Parse`

```json
{"User":"Test","Course_Id":"course_001"}
```

响应：`{"result":"Parse_Task","task_id":"..."}`。解析器使用 LibreOffice/PDFBox 生成页面图片，并提取页面文本和备注。

## 场景和讲稿

`GET /Course_Scenes?course_id=course_001` 返回页面缩略图、文本和讲稿草稿。

`PUT /Course_Scenes` 保存用户修改：

```json
{"User":"Test","Course_Id":"course_001","Scenes":[{"Index":1,"Script":"本页讲稿"}]}
```

## 生成和下载

`POST /Course_Render` 提交完整管线：讲稿转语音、字幕时间轴、SadTalker/MuseTalk、FFmpeg 合成 MP4。

`POST /Course_State` 查询阶段：`parse`、`script`、`tts`、`avatar`、`merge`。

`GET /Course_Download?course_id=course_001&type=mp4` 返回服务器本地文件流；`type=vtt` 返回字幕文件。当前不上传 OSS/MinIO。
